<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. -->
<project basedir="." default="build" name="sedec2" >
    <property environment="env"/>
    <property name="version" value="2.0"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="1.7"/>
    <property name="source" value="1.7"/>
    <property name="release.dir" value="out"/>
    <property name="bin.dir" value="bin"/>
    <property name="lib.dir" value="libs"/>
    <property name="src.dir" value="src"/>
    <property name="doc.dir" value="${release.dir}/doc"/>
    <path id="project.classpath">
        <fileset dir="${lib.dir}">
            <include name="**/*.jar" />
        </fileset>
    </path>
              
    <target name="init">
        <mkdir dir="${bin.dir}"/>
        <mkdir dir="${release.dir}"/>
        <copy includeemptydirs="false" todir="${bin.dir}">
            <fileset dir="${src.dir}">
                <exclude name="**/*.launch"/>
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>
              
    <target name="clean">
        <delete dir="${bin.dir}"/>
        <delete dir="${release.dir}"/>
    </target>              
              
    <target depends="build-project" name="build">
        <tstamp>
            <format property="builtat" pattern="MM/dd/yyyy hh:mm aa" />
        </tstamp>        
        <exec executable="whoami" outputproperty="whoami"/>
        <exec executable="uname" outputproperty="buildsystem"><arg value="-a"/></exec>
        <propertyfile file="${basedir}/version.properties"
                comment="This file is automatically generated - DO NOT EDIT">
            <entry key="buildtime" value="${builtat}"/>
            <entry key="builder" value="${whoami}"/>
            <entry key="version" value="${version}"/>
            <entry key="build.number" default="0000" operation="+" pattern="0000" type="int"/>
            <entry key="system" value="${buildsystem}"/>
        </propertyfile>
    </target>
              
    <target depends="init" name="build-project">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="${bin.dir}" includeantruntime="false" source="${source}" target="${target}">
            <src path="src"/>
            <classpath refid="project.classpath"/>
        </javac>
    </target>
              
    <target depends="init, build" name="release-arib-tlv-decoder">
        <jar jarfile="${release.dir}/${ant.project.name}-${version}-example-arib-tlv-decoder.jar" 
            basedir="${bin.dir}" 
            includes="**">
            <manifest>
                <attribute name="Main-Class" value="zexamples.arib.TlvPacketDecoder"/>
            </manifest>
            <zipgroupfileset dir="${lib.dir}" includes="**/*.jar"/>
        </jar>
    </target>

    <target depends="init, build" name="release-arib-section-decoder">
        <jar jarfile="${release.dir}/${ant.project.name}-${version}-example-arib-section-decoder.jar" 
            basedir="${bin.dir}" 
            includes="**">
            <manifest>
                <attribute name="Main-Class" value="zexamples.arib.AribTableDecoder"/>
            </manifest>
            <zipgroupfileset dir="${lib.dir}" includes="**/*.jar"/>
        </jar>
    </target>

    <target depends="init, build" name="release-dvb-section-decoder">
        <jar jarfile="${release.dir}/${ant.project.name}-${version}-example-dvb-section-decoder.jar" 
            basedir="${bin.dir}" 
            includes="**">
            <manifest>
                <attribute name="Main-Class" value="zexamples.dvb.DvbTableDecoder"/>
            </manifest>
            <zipgroupfileset dir="${lib.dir}" includes="**/*.jar"/>
        </jar>
    </target>

    <target depends="init,build-project" name="release-sdk">
        <jar jarfile="${release.dir}/${ant.project.name}-${version}.jar" 
            basedir="${bin.dir}" 
            includes="**/*.class">
        </jar>
    </target>
    
    <!-- @note : http://blog.daum.net/_blog/BlogTypeView.do?blogid=05gBm&articleno=7019813 -->
    <target depends="release-sdk" name="release-sdk-obfuscate">
        <taskdef resource="proguard/ant/task.properties"
                 classpath="${lib.dir}/proguard-6.0.3.jar" />
        <echo> ${java.home} </echo>
        <proguard overloadaggressively="on"
                 defaultpackage="${ant.project.name}"
                 allowaccessmodification="on"
                 printseeds="on"
                 obfuscate="yes">
            -libraryjars ${java.home}/lib/rt.jar
            -injars ${release.dir}/${ant.project.name}-${version}.jar
            -outjars ${release.dir}/${ant.project.name}-${version}.min.jar
            -dontnote
            -dontwarn
            -keep class ** { 
                public *;
            }
            -keep interface ** {
                public *;
            }
            -keepattributes InnerClasses
         </proguard>
    </target>

    <target depends="release-sdk-obfuscate,doc" name="deploy">
        <property name="id" value="root"/>
        <property name="server_ip" value="10.0.12.157"/>
        <property name="server_path" value="/var/www/html/thkang2"/>
        <exec executable="rsync">
            <arg value="-avz"/>
            <arg value="${release.dir}"/>
            <arg value="${id}@${server_ip}:${server_path}/${ant.project.name}"/>
        </exec>
    </target>
	
    <target name="doc" description="generate documentation">
        <javadoc destdir="${doc.dir}" 
                classpathref="project.classpath" 
                version="true"
                windowtitle="${ant.project.name} SDK Document" 
                doctitle="${ant.project.name} SDK which's able to extract everything from TLV"
                linksource="yes"
                encoding="utf-8"
                charset="utf-8"
                docencoding="utf-8"
                access="public"
                bottom="Copyright TehoKang. All rights reserved.">
            <fileset dir="${src.dir}" defaultexcludes="no"/>
            <classpath>
                <pathelement location="${bin.dir}"/>
            </classpath>
        </javadoc>
    </target>
</project>
